
# Backtest artifacts — signal caches and reports are large and regenerable
# Runtime state — regenerated by the bot, not source
*.log
*.pyc
*.pyo
.DS_Store
.env
.venv/
.venv*/
__pycache__/
backtest_reports/*.json
last_picks.json
last_picks_s2.json
last_picks_s3.json
picks_log.json
position_peaks.json
push_subs.json
raanu.db*
server_run.log
trades_log.json
tools/__pycache__/

# AWS CDK app (aws/) — build/staging artifacts, not source
aws/cdk.out/
aws/cdk.context.json
aws/.venv/
aws/node_modules/
# Regenerated from RaanuTradingBot.html/sw.js/manifest.webmanifest/icons/ at
# every `cdk synth`/`cdk deploy` — see skeleton_stack.py's
# _stage_dashboard_site(). Those repo-root files stay the single source of
# truth for both Railway and AWS; this is a build output, not source.
aws/site/

# Root JS tooling (dashboard linter) — see package.json
/node_modules/
.dashboard-inline.js
